/** @type {import('next').NextConfig} */

const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
}
module.exports = nextConfig

module.exports = {
  images: {
    // domains: ['wp.zelisline.com'],
    unoptimized: true,

  },
};

